#pragma once

#ifndef MKIN_H
#define MKIN_H

#include <bits/stdc++.h>
using namespace std;

// ═══════════════════════════════════════════════════════════════════
// 总测试点数量 & 测试类型注释
// ═══════════════════════════════════════════════════════════════════
// 25 组测试点，无 subtask 分组，每点独立计分
// 
// 测试类型分布（仅供注释参考）：
//   case 1-2:  样例数据
//   case 3-8:  小规模 + 特殊性质
//   case 9-11: Hack 数据
//   case 12-20: 中大规模
//   case 21-25: 随机回归
// ═══════════════════════════════════════════════════════════════════

const int TEST_CASES = 25;

// ═══════════════════════════════════════════════════════════════
// ⚠️ 只看这里：改 mkin.h，别动 mkdata.cpp！
// ═══════════════════════════════════════════════════════════════
// 
// ✅ 改这个文件（mkin.h）里的 test() 函数
// ❌ 别动 mkdata.cpp（框架代码，不需要改）
//
// 测试数据说明（25组，无分组，每点独立计分）：
// ═══════════════════════════════════════════════════════════════

/**
 * 测试数据生成函数
 * 
 * 参数：
 *   case_num: 测试点编号（1-25）
 *   fout: 输出文件流（写入 .in 文件）
 * 
 * 特殊性质数据设计参考：
 *   - 单调性：递增/递减序列，测试排序、二分等算法
 *   - 所有值相同：测试重复值处理
 *   - 极值：最大/最小可能值，测试溢出
 * 
 * Hack 数据设计参考：
 *   - int 溢出：使用 > 2^31-1 的数据，使 int 错误
 *   - 边界漏判：N=1, N=max 等，测试边界处理
 *   - 超时：使 O(n²) 算法超时
 *   - 错误贪心：使贪心策略失败的数据
 */

void test(int case_num, ofstream& fout)
{
    // ============================================================
    // Subtask 0: 样例数据（直接复制题目样例） — case 1-2
    // ============================================================
    if (case_num == 1)
    {
        // TODO: 样例1 - 从题面复制
        // fout << "..." << endl;
    }
    else if (case_num == 2)
    {
        // TODO: 样例2 - 从题面复制
        // fout << "..." << endl;
    }
    
    // ============================================================
    // Subtask 1: 小规模随机数据（最小 N，验证基本功能）— case 3-5
    // ============================================================
    else if (case_num >= 3 && case_num <= 5)
    {
        // TODO: 生成小规模随机数据
        // 建议：N 在 [1, 10] 范围内
        // int N = rand() % 10 + 1;
        // fout << N << endl;
        // for (int i = 0; i < N; i++) { ... }
    }
    
    // ============================================================
    // Subtask 1(续): 特殊性质数据 — case 6-8
    // ============================================================
    else if (case_num == 6)
    {
        // TODO: 特殊性质1 - 单调性（递增）
        // 适用于：需要排序、二分查找等题目
        // int N = 100;
        // fout << N << endl;
        // for (int i = 0; i < N; i++) fout << i << " ";
    }
    else if (case_num == 7)
    {
        // TODO: 特殊性质2 - 所有值相同
        // 适用于：测试是否正确处理重复值
        // int N = 1000;
        // fout << N << endl;
        // for (int i = 0; i < N; i++) fout << "5 ";
    }
    else if (case_num == 8)
    {
        // TODO: 特殊性质3 - 递减序列 / 极值情况
        // 根据题目的特殊性质定制
    }
    
    // ============================================================
    // Subtask 2: Hack 数据（针对常见错误写法）— case 9-11
    // ============================================================
    else if (case_num == 9)
    {
        // TODO: Hack 1 - 整数溢出测试
        // 针对：使用 int 而非 long long 的错误
        // int N = 2;
        // fout << N << endl;
        // fout << "2147483647 1" << endl;  // int 最大值
    }
    else if (case_num == 10)
    {
        // TODO: Hack 2 - 边界漏判测试
        // 针对：未处理 N=1 或 N=max 的情况
        // int N = 1;  // 最小边界
        // fout << N << endl;
        // ...
    }
    else if (case_num == 11)
    {
        // TODO: Hack 3 - 超时测试 / 错误算法测试
        // 针对：O(n²) 或错误贪心
        // 生成使错误算法失败的数据
    }
    
    // ============================================================
    // Subtask 3: 中等规模数据（验证效率）— case 12-15
    // ============================================================
    else if (case_num >= 12 && case_num <= 15)
    {
        // TODO: 生成中等规模数据
        // 建议：N 在 [100, 10000] 范围内
    }
    
    // ============================================================
    // Subtask 3(续): 大规模数据（压力测试）— case 16-20
    // ============================================================
    else if (case_num >= 16 && case_num <= 20)
    {
        // TODO: 生成大规模数据
        // 建议：N 接近题目限制上限
        // 验证程序在极限情况下的表现
    }
    
    // ============================================================
    // Subtask 4: 随机回归测试 — case 21-25
    // ============================================================
    else
    {
        // TODO: 随机生成测试数据
        // 覆盖各种情况，确保程序稳定性
    }
}

#endif
